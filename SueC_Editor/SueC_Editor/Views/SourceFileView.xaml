<UserControl x:Class="SueC_Editor.Views.SourceFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SueC_Editor.Views"
             xmlns:viewModel="clr-namespace:SueC_Editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:SourceFileViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Left">
            <ItemsControl ItemsSource="{Binding Path = SourceFiles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding Path = Title, Mode=OneWay}" Width = "100" Margin = "3 5 3 5"/>
                            <TextBox Text="{Binding Path = PathName, Mode=OneWay}" Width = "100" Margin = "3 5 3 5"/>
                            <TextBox Text="{Binding Path = Content, Mode=TwoWay}" Width = "100" Margin = "3 5 3 5"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
